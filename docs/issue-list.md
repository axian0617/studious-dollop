# Issue List

## 新一轮问题

### P0-A 本轮必须修

1. 登录页布局细节错误
   - logo 和“银联商务”需要上下居中成组，logo 在上，文字在下。
   - 确认登录按钮必须收回登录卡片内部，登录卡片不能内容溢出。
   - 登录页保持 854×480 TerminalFrame 内完整显示。
   - 禁止恢复账号密码登录、密码输入框、账号密码登录大标题、左侧品牌展示区、宣传文案、Powered by UMS、854×480 Landscape UI、左右分栏。

2. 《银联商务用户隐私协议》必须可点击
   - 协议文字需要是链接样式。
   - 点击后在 854×480 TerminalFrame 内弹出协议弹窗。
   - 弹窗标题：银联商务用户隐私协议。
   - 内容使用 mock 文案，有关闭按钮，不超出 TerminalFrame。

3. 绑定身份角色流程内容缺失
   - 第一步选择身份角色：老板、财务、收银员，每个角色有对应 icon，选中状态清晰，点击下一步进入绑定信息页。
   - 第二步绑定商户信息必须包含：是否有营业执照有 / 无、营业执照号输入框、商户号输入框、门店号输入框、操作员输入框。
   - “是否有营业执照”必须是明确选择控件。
   - “营业执照号”输入框必须出现；选择无营业执照时可以禁用或隐藏，但控件逻辑必须体现。
   - 必填项需要红色 *。
   - 当前身份需要根据上一步选择显示。

4. 扫码绑定功能只做展示
   - 可以保留扫码绑定按钮。
   - 点击后只弹出 toast 或 mock 弹窗，例如“扫码绑定功能仅作演示”。
   - 不新增扫码页面，不调用摄像头，不接真实接口。

5. 绑定完成后角色需要同步到首页
   - 选择老板后首页显示老板。
   - 选择财务后首页显示财务。
   - 选择收银员后首页显示收银员。
   - 不写死管理员，角色来自统一 mock state / app state。

6. 所有新增弹窗和键盘必须限制在 TerminalFrame 内
   - 协议弹窗、验证码输入键盘、手机号输入键盘、扫码绑定演示 toast / 弹窗都必须限制在 854×480 终端画布内。
   - 禁止使用 fixed、100vw、100vh、w-screen、h-screen、挂到浏览器 viewport 的 inset-0。

### P0-B 本轮修复

1. 今日交易时间筛选点击后需要弹出“时”和“分”的选择框。
2. 今日交易金额筛选需要金额输入键盘。
3. 今日交易支付方式筛选里的支付宝、微信、云闪付、数字人民币图标需要使用对应支付方式的正确 logo 或至少是清晰可识别的 mock icon。
4. 今日交易筛选模块边缘不需要描边。
5. 收款语音播报页面内容需要严格参考上传的“收款语音播报”交互稿重做。

### P1 本轮只记录，不要修改

1. 首页六个支付方式金额数字仍然要使用粗字重，和左侧今日收款金额字重一致。
2. 首页六个支付方式金额不要五颜六色，金额用正常深色即可。
3. 首页六个支付方式名称左侧增加一个带颜色的小圆点作为区分。
4. 首页今日收款金额右侧的进入箭头图标去掉。
5. 首页商户服务模块四个功能贴底了，商户服务模块高度应该再长一点。

## P0-A 已修复

1. 登录页流程错误
   - 当前登录页是账号密码登录，但原交互稿要求是手机号 + 验证码登录。
   - 需要改为：手机号输入框、验证码输入框、发送验证码按钮、隐私协议勾选、确认登录按钮。
   - 必须删除：“账号密码登录”、密码输入框、默认测试账号 18889008888 / 666888、左侧品牌展示区、左右分栏、宣传文案、大面积 logo 色块。
   - 登录页只保留：logo、银联商务、手机号、验证码、阅读隐私协议、确认登录。

2. 登录输入键盘缺失
   - 点击手机号输入框或验证码输入框时，需要弹出 Demo 内模拟数字键盘。
   - 键盘必须在 854×480 TerminalFrame 内部，可以从底部弹出。
   - 包含数字 0-9、删除、完成。
   - 点击完成后关闭键盘。
   - 不允许键盘超出 TerminalFrame。
   - 不调用浏览器原生键盘，不依赖移动端系统键盘。

3. 首次登录后缺少绑定商户信息流程
   - 登录成功后不能直接进入首页。
   - 首次登录必须进入绑定身份 / 绑定商户流程。
   - 需要体现：选择角色老板、财务、收银员；每个角色有对应 icon；选择角色后进入商户绑定信息填写 / 确认流程；绑定成功后进入首页。
   - 非首次登录才可以直接进入首页。
   - 使用 mock 状态：firstLogin = true 时，登录后进入绑定流程；绑定完成后 firstLogin = false，再进入首页。

4. 全局角色文案不能写死
   - 角色需要根据绑定身份显示：老板、财务、收银员。
   - 影响位置：首页顶部商户信息、切换店铺 / 角色页面、其他显示当前角色的位置。
   - 当前角色来自统一 mock state / app state。
   - 不在页面中写死“管理员”。

5. 所有弹层必须限制在 854×480 终端画布内
   - 检查并修复：Drawer、Modal、Toast、Dropdown、Select popup、Confirm dialog、Loading overlay、数字键盘。
   - 禁止业务 UI 使用：position: fixed、100vw、100vh、w-screen、h-screen、挂到浏览器 viewport 的 inset-0。
   - TerminalFrame 必须是：width: 854px; height: 480px; position: relative; overflow: hidden。
   - 今日交易详情 Drawer、筛选浮层、数字键盘都不能超出 854×480。

6. 删除测试占位图
   - 删除页面右下角出现过的奇怪猫图 / 测试图片 / placeholder 图片。
   - 真实业务页面不允许出现测试占位图。

## P0-B 已修复

1. 今日交易筛选功能不完整
   - 今日交易页面顶部筛选条件应包括：支付方式、时间、金额。
   - 支付方式筛选：点击后弹出浮层，展示六种支付方式的 icon + 名称，支持多选，包括云闪付、支付宝、微信、借记卡、贷记卡、数字人民币。
   - 时间筛选：可以选择今天 00:00 到 23:59 之间任意时段，需要起始时间和结束时间。
   - 金额筛选：点击后弹出金额条件弹窗，包含最小金额输入框、最大金额输入框，可以确认和取消。
   - 所有筛选浮层必须限制在 854×480 TerminalFrame 内。

2. 今日交易列表中，每条支付方式左侧需要对应 icon。

## P1 已修复

1. 首页今日收款金额右侧需要显示/隐藏金额图标。
2. 所有金额数字采用 Arial 字体：大号金额 bold，小号金额 regular。
3. 首页六个支付方式金额用对应颜色区分。
4. 支付方式名称旁边补单位“元”，例如：微信（元）。
5. 切换店铺 / 角色列表有点拥挤，店铺名称和小字层级需要拉开。
6. 消息中心管理中，“新消息通知”下面应增加小字“消息推送类型”，然后才展示三种消息类型。
7. 消息通知每个模块左侧需要对应消息类型 icon。

## P2 已做轻量修复

1. 登录页整体还可以更清爽。
2. 首页卡片层级后续再微调。
3. 蓝色使用后续可以更克制。
4. 阴影和圆角后续统一精修。
